# aiffel-quests 리포 규약

아이펠 AI 에이전트 과정의 **과제(퀘스트)를 실제로 구현하는** 리포. 코드가 자산이다.

## AI-Study 와의 경계

| | `AI-Study` | `aiffel-quests` (여기) |
|---|---|---|
| 자산 | 사람이 읽는 노트 (`notes/`) | 돌아가는 코드 + 제출물 |
| 만드는 것 | 커리큘럼 요약·강의노트 | PRD, 구현, 배포, 실험 결과 |
| 스킬 | `curriculum-note`, `yt-digest` | 없음 — 일반 개발 세션 |

**두 리포는 서로의 파일을 건드리지 않는다.** 여기서 강의노트를 쓰지 않고, `AI-Study`에
코드를 두지 않는다. 참조가 필요하면 상대 리포의 경로를 문서에 적기만 한다 (심링크·서브모듈 금지).

노트에서 과제 구현이 궁금해지면 이 리포로 세션을 옮기고, 구현 중 커리큘럼 원문이 필요하면
`AI-Study/notes/aiffel/m<NN>-*.md` 를 **읽기만** 한다.

## 구조

```
m<2자리>-<모듈 제목 slug>/   모듈별 퀘스트 하나 = 폴더 하나
  PRD.md                     사용자 관점의 요구사항 (유즈케이스·성공 기준·안 할 것)
  PLAN.md                    수직 슬라이스 목록 + 순서 + 완료 기준
  README.md                  과제 개요·실행법·배포 주소·제출물 체크리스트
  <구현>                     앱 코드. 스택은 퀘스트마다 다르다
```

### 폴더 이름은 강의노트 파일명에서 그대로 가져온다

**`AI-Study/notes/aiffel/<파일명>.md` 에서 `.md` 를 뗀 것이 폴더 이름이다.** 노트 파일명이
이미 `m<2자리>-<모듈 제목 kebab slug>` 규칙을 따르므로, 변환하거나 줄이지 않고 그대로 쓴다.

```
notes/aiffel/m05-rag-chatbot-service.md   →   m05-rag-chatbot-service/
notes/aiffel/m03-rag-pipeline.md          →   m03-rag-pipeline/
```

이렇게 두면 노트 ↔ 구현을 양쪽에서 문자열 하나로 찾을 수 있다. 폴더 이름이 마음에 안 들면
**노트 파일명을 먼저 고치고** 폴더를 맞춘다 — 반대 방향으로 어긋나게 두지 않는다.

노트가 아직 없는 모듈의 과제를 먼저 시작하게 되면, 모듈 제목을 소문자 kebab-case로 옮겨
`m<NN>-<slug>` 를 직접 짓고 그 이름을 나중에 노트에도 쓴다. 공백·한글 폴더명은 쓰지 않는다
(본문은 한국어).

새 최상위 폴더를 만들지 않는다. 여러 퀘스트가 공유하는 코드가 생기면 그때 `shared/` 를 논의한다.

## 작업 순서

전역 규칙의 **문제정의 → PRD → 작업계획 → 구현 → 정리** 를 그대로 따른다. 퀘스트마다
`PRD.md` → `PLAN.md` 를 먼저 만들고, 구현 세션은 그 파일을 읽는 것으로 시작한다.

- 슬라이스 하나가 끝나면 `PLAN.md` 체크 → 커밋
- 슬라이스 중간에 멈추면 `PLAN.md` 아래 인계 메모 세 줄 (한 것 / 남은 것 / 막힌 것)
- **"된다"는 실행해서 확인한 것만** — 브라우저 앱이면 실제로 띄워서 동작을 본다

## 커밋

메시지 앞에 모듈 접두어를 붙여 어느 퀘스트인지 한눈에 보이게 한다.

```
m05: 청크 스키마와 ID 규칙 확정
m05: 코사인·BM25 하이브리드 검색 붙임
```

리포 전체에 걸친 변경(규약·gitignore 등)은 접두어 없이 쓴다.

## 커밋하지 않는 것

- API 키·토큰 — `.env` 는 gitignore 대상이고, `.env.example` 에는 **키 이름만** 둔다
- 임베딩 모델 가중치, `node_modules/`, 빌드 산출물 (`dist/`)
- 벡터스토어 JSON 은 **커밋한다** — 정적 배포에 필요한 산출물이고 크기가 작다면

## 제출물

퀘스트마다 `README.md` 하단에 제출 체크리스트를 두고, 제출 직전에 항목을 실행으로 확인한다.
배포 주소는 README 최상단에 적는다.
