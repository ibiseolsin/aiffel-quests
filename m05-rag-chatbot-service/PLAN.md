# PLAN — m05 식품 표시·광고 규정 안내 챗봇

> 요구사항은 [README](README.md), 사용자 관점 요구는 [PRD](PRD.md), 실측은 [FINDINGS](FINDINGS.md).
> 슬라이스 하나 = 세션 하나. 끝나면 여기 체크하고 커밋한다.
> 구현 세션 시작 프롬프트: `PRD.md, PLAN.md 읽고 슬라이스 N 진행. 끝나면 PLAN.md 체크하고 커밋.`

## 무엇을 만드는가

> **"내가 만든 식품을 판매채널에 올릴 때, 표시사항과 광고 문구가 규정에 맞는가"**
> 를 **실제 법령 조문을 근거로** 답하고, 그 조문·검색 경로·판정을 화면에 드러내는 정적 챗봇.

도메인 근거 — M01 「도메인 로드맵」의 선언 *"나는 소비자가 원하는 식품을 찾아내고 제조하고
판매하는 사람이다"* 중 **①-3 판매채널 등록·마케팅 + ②-3 제품 마케팅**.
(①-1 바이럴 분석은 실시간 데이터가 필요해 M04, ①-2 영업보조는 비공개 정보라 M08 의 몫.)

M11 「식품 신제품 기획 툴」의 **"이 문구가 규정에 맞는가" 모듈**로 재사용된다.

## 확정된 결정

| # | 결정 |
|---|---|
| D5 | **Gemini** + 사용자가 화면에서 키 입력. 브라우저 직접 호출 CORS 실측 확인. 키는 브라우저에만 |
| D6 | 반응형 — **처음부터 단일 컬럼**. 별도 슬라이스 없음 |
| D7 | **`aiffel-quests` 모노리포 유지.** Actions 가 이 폴더만 빌드해 Pages `/m05/` 로 |
| — | **코사인 + BM25 하이브리드.** 근거: 조문 번호가 질문에 그대로 등장하고("제8조 제1항 제3호"), 법령 용어에 한자 병기가 섞여 있다. 임베딩이 놓치는 정확한 표기를 BM25 가 잡는다 |
| — | **청크는 빌드 시 사전 계산, 질의는 브라우저에서.** 키 없는 방문자도 검색·근거·조문 링크까지 쓸 수 있다 |
| — | **법률 자문이 아님을 화면에 상시 표시.** 이 도메인은 틀린 답의 대가가 법적이다 |

## 슬라이스

- [x] **S0. PRD 재작성** — PRD 1·2·4·9절과 FINDINGS 3·4절을 이 도메인으로 교체.
      법률 자문 아님을 비목표·화면 규칙에 추가.
      *완료 기준*: PRD 리뷰 확인 목록 8개를 새 도메인에서 통과. `"법령에 없다"` 류 표현이
      되살아나지 않았는지 확인 (코퍼스 ≠ 전체 법령)
      → **완료.** 확인 목록 8개 기계 검사 통과. `"법령에 없다"` 는 전부 *금지하는* 문맥으로만
      등장(5곳). `D8` 번호 충돌과 `D6` 모순(결정 필요 ↔ 확정됨)을 발견해 수정.
      독립 리뷰 에이전트 1회 실행 — 결과는 아래 «S0 리뷰» 참고

- [x] **S1. 껍데기와 배포 파이프라인** — Vite+React+TS 정적 앱, 상대 경로 base,
      GitHub 원격 + Actions → Pages `/m05/`.
      *완료 기준*: 공개 URL 을 다른 브라우저 프로필에서 열어 화면이 뜬다
      → **완료.** https://ibiseolsin.github.io/aiffel-quests/m05/
      Actions `build 11s / deploy 10s` 성공. HTML·CSS·JS 전부 200, 콘솔 에러 없음,
      자산 경로가 `./assets/` 상대 경로로 나옴(하위 경로 배포 확인).
      **막혔던 것**: CLI 토큰에 `workflow` 스코프가 없어 워크플로우 파일 push 가 거부됐다.
      원인이 둘이었다 — ① 스코프 부재 ② git 이 `credential.helper=manager`(GCM)로 `gh` 와
      **다른 토큰**을 씀. ②는 이 리포만 `!gh auth git-credential` 로 돌려 해결했고,
      ①은 워크플로우 파일을 **웹 에디터로 별도 생성**해 우회했다.
      → **앞으로 워크플로우를 고칠 때도 CLI push 로는 안 된다.** 웹 에디터를 쓰거나
      `gh auth refresh -s workflow` 를 먼저 통과시켜야 한다

- [x] **S2. 코퍼스 — 법령 수집과 청크** (D2 확정) — 법령 API 로 표시광고법 3종 + 고시를 받아
      **조/항/호** 단위로. `조문가지번호` 를 ID 에 반영(제8조 vs 제8조의2). **고시 별표 포함**.
      본인 `OC` 발급 후 `.env` (커밋 금지).
      **⚠ `[현행]` 이 "지금 시행 중"을 뜻하지 않는다** — 「표시기준」은 시행 2028-01-01 개정본이
      `[현행]` 으로 온다(FINDINGS 함정 ③). 시행 중인 판을 가져오는 경로를 확정하거나, 청크마다
      시행일을 넣고 오늘 기준으로 시행 중/예정을 계산한다. **검색 결과 날짜가 아니라 본문에서 읽는다.**
      *완료 기준*: ① 무작위 5개의 인용 URL 을 클릭해 law.go.kr 에서 그 조문을 찾을 수 있다
      ② **Q1~Q9 각각의 근거 조문을 코퍼스에서 지목할 수 있다** (못 찾은 Q 는 여기서 유형을 바꾼다)
      ③ 시행 예정 청크가 식별된다 ④ 최종 법령 목록과 청크 수를 여기 기록
      → **완료. 365 청크, 전부 시행 중.**
      ① 무작위 8개 URL 200 확인 (법률·시행령·시행규칙·고시 4종 모두)
      ② Q1~Q7 근거 확보, Q8·Q9 근거 0건(기대대로). **Q7 은 키워드만 걸리고 실질 근거가 없어
         「일부 범위 밖」 → 「코퍼스 밖」 으로 정정**
      ③ 시행 예정 개정 3건을 식별해 코퍼스에서 배제하고 보고서에 남김
      ④ **청크 분할기를 다시 씀** — 고시 본문에서 머리말이 뒤 계층 마커에 잘려 나가
         「…열량 표시 제 외)」 같은 두 동막 청크가 나왔고, 11자 조각이 236개 있었다.
         마커 판별을 공백에서 **순번(1부터 오르는 목록)** 으로 바꾸고, 머리말은 재귀에서
         제외하고, 짧은 형제는 묶는다. 상세는 [FINDINGS](FINDINGS.md) 함정 ⑤
      **찾은 버그 2개**: `target=law` 로 시행규칙의 낡은 판(2024-12-30)을 받고 있었다
      → `target=eflaw` 로 교체(2026-01-01). 인용 URL 이 `제8조①` 로 생성돼 엉뚱한 곳으로 갔다
      → 조 단위로 수정. 상세는 [FINDINGS](FINDINGS.md) 함정 ③·④

- [x] **S3. 벡터스토어 + 브라우저 질의 임베딩** — 빌드 시 청크 임베딩, 브라우저는 질의만.
      같은 모델·pooling·정규화. 캐시와 진행률. **동일 공간 검증**(청크 재임베딩 → 코사인 ≈ 1).
      *완료 기준*: **엔진 없이** 상위 조문과 점수가 화면에 나온다. 캐시 지우고 재접속 시 진행률(A8)
      → **완료.** 로컬 preview 에서 확인: 캐시 삭제 → 재접속 → 진행률(27% · 34MB/129MB · 파일 4개)
      → 검색 20~64ms, 상위 8개 조문과 코사인 점수, 조문별 law.go.kr 링크와 시행일.
      **동일 공간 검증 통과 (코사인 0.9992)** — 1.0 이 아닌 것은 WASM↔네이티브 수치 차이.
      **찾은 버그 1개 (조용한 것)**: 청크를 16개씩 배치로 임베딩하면 패딩이 mean pooling 에
      섞여 **벡터가 배치 동료에 의존**한다 (단건 재현 코사인 0.977). 단건 임베딩으로 바꾸고
      빌드에 재현성 자체 점검을 넣었다. 이 버그가 **모델 비교 결과까지 오염**시켰다 —
      오염 상태에서 6/7 이던 e5-base 가 고치니 5/7 이었다.
      **모델은 e5-small 로 확정** (계획의 embeddinggemma 를 쓰지 않는다): 셋 다 상위5 적중 5/7
      동률인데 e5-small 이 129MB, gemma 205MB, e5-base 282MB. 상세는 [FINDINGS](FINDINGS.md) 5절

- [x] **S4. 하이브리드 검색** — BM25 추가, 최고점 정규화, 경로 보존, 중복 제거.
      *완료 기준*: **조문 번호를 직접 묻는 질문**이 BM25 경로로 잡히는 것을 화면에서 확인
      → **완료.** 「시행규칙 제6조 제2항 제3호에 뭐가 적혀 있나요」에서 정답 조문이
      **의미 검색 20위 · 표기 검색 2위 → 하이브리드 1위**. 의미 검색만의 1·2위였던 무관한
      조문은 5·6위로 내려갔다. 근거마다 두 경로의 순위를 화면에 적어 이게 보이게 했다.
      실측으로 정한 것 셋: **희소가중치 0.3** (0.15~0.4 에서 적중 동일 — 칼날 위가 아니다),
      **후보 30개** (80으로 넓히면 7/8 → 6/8 로 **떨어진다**), **한글 2-gram 사용**
      (끄면 7/8 → 6/8). 중복 제거는 앞 150자를 공유하는 형제 무리를 **무리당 2개로 제한** —
      「표시기준」은 식품유형마다 같은 목록을 반복해 최대 20개가 한 무리다.
      **평가 세트 정답 정의 하나를 바로잡았다**: Q5(SNS 협찬)의 정답을 「광고」 정의 조항만으로
      뒀는데, 검색이 1위로 가져온 **제3조(다른 법률과의 관계)** 가 「이 법 적용을 받나요」에
      더 직접적인 답이었다. 정답 정의가 좁았던 것이고, 그래서 넓혔다.
      **남은 실패**: Q1(「면역력 강화」→ 제8조①제1호 질병 예방·치료)은 어느 가중치에서도
      상위 5에 안 들어온다. 어휘가 겹치지 않아 BM25 가 못 잡고, e5-small 이 의미로도 못 잇는다.
      S11 의 top-k 축과 쿼리 확장에서 다시 본다

- [~] **S5. 프롬프트 조립 + 스트리밍 + 취소** — 순서: 자료 소개 → 근거 원칙 →
      `[ID]` 인용 요구 → 법령 시행일·현재 시각 → 자료 → 질문. 청크는 법령명·위치·URL 유지.
      *완료 기준*: 답변이 스트리밍되고 조문 인용이 붙는다. 취소 동작(A7). **배포본에서도 된다**
      → **로컬 확인 완료, 배포본·Gemini 는 미확인.**
      확인한 것: Ollama(`qwen3.5:2b`)로 브라우저에서 스트리밍 — 첫 글자 1374ms · 전체 9.1초,
      인용 S1~S7 전부 유효. **취소** — 중단 후 2.5초간 1글자만 늘고 멈춤(대기 중이던 토큰).
      키 없이 Gemini 를 고르면 크래시 없이 안내가 뜨고 **근거 8개는 그대로 보인다**
      (자동 폴백 없음).
      **배포본 확인 완료** (https://ibiseolsin.github.io/aiffel-quests/m05/): 모델 다운로드 →
      동일 공간 검증 통과(코사인 0.9992) → 검색 48ms, 하이브리드 순위가 로컬과 **동일**
      (시행규칙 제6조②제3호: 의미 20위 · 표기 2위 → 1위).
      **CORS 미확인을 닫았다 (2026-08-29).** 자격증명 없이도 확인할 수 있는 부분이었다 —
      일부러 틀린 자격증명으로 부르고 **상태 코드와 본문이 읽히는지**를 봤다. 읽혔다면
      CORS 는 통과한 것이다(막히면 브라우저가 응답 대신 `TypeError` 를 던진다).
      배포본 origin 에서 **넷 다 통과**: `oauth2` 토큰 교환 400 · AI Studio 400 ·
      Vertex `Bearer` 401 · Vertex API 키 401.
      **그런데 익스프레스 키만 실패 이유가 다르다** — 앞의 셋은 「이 자격증명이 틀렸다」인데
      익스프레스 키는 **「이 API 는 API 키를 안 받는다」**(`API keys are not supported by
      this API`)고 답한다. 키의 유효성이 아니라 엔드포인트의 성질에 대한 말이고, 헤더·
      쿼리스트링·스트리밍·비스트리밍 **네 조합이 전부 같은 401** 이었다. 익스프레스 모드로
      *발급된* 키는 다를 수 있어 창구는 지우지 않고, **이 401 이 뜨면 무엇을 봐야 하는지
      화면에서 짚어 주도록 고쳤다**(자주 갈리는 실패 셋 + 본문은 늘 그대로 이어 붙임 +
      `pre-line`). 로컬에서 두 창구 다 눌러 확인했고, 그때도 **근거 8개는 그대로 남는다**.
      상세는 [FINDINGS](FINDINGS.md) 8절
      **Gemini 답변 경로가 닫혔다 (2026-08-29, 사용자 확인).** AI Studio 키 +
      `gemini-3.7-flash` 로 **배포본에서 정상 동작**. 에이전트가 직접 실행해서 본 것은
      아니다 — 키가 사용자에게만 있다. 모델 ID 가 AI Studio 창구에 실재한다는 것도 같이
      닫혔다(401·400 이 모델 이름을 보기 전에 나와서 여태 걸러지지 않던 것이다).
      그래서 **기본 창구를 `vertex-sa` → `studio` 로 바꿨다** — 실제로 답변이 온 것이
      확인된 경로가 기본이어야 하고, 서비스 계정 JSON 은 개인키가 브라우저에 올라가고
      권한도 넓다.
      **미확인 ②(배포본에서 Ollama)는 S9 에서 닫혔다 — 결과는 「안 된다」다.** 관문이 둘이고
      `OLLAMA_ORIGINS` 로는 못 넘는다 (FINDINGS 9절). **남은 미확인은 ① 하나뿐이다**:
      **Gemini 가 인용을 어떤 표기로 붙이는가**. Ollama 2B 는 붙이되 `(S1, S5)` 로 썼다.
      그리고 S10 에서 **같은 모델이 같은 질문에서도 표기를 바꾼다**는 것이 나왔다
      (FINDINGS 10절) — 「모델마다 갈린다」보다 나쁜 상황이고 S6·S7·S8 이 이 위에 선다

      > **인계 메모** — 한 것: Gemini 세 창구의 CORS 실측, 익스프레스 키 창구가 키를 받지
      > 않는다는 판정, 오류 화면의 원인 짚기, AI Studio 창구로 배포본 답변 확인(사용자).
      > 남은 것: Gemini 답변에 인용이 붙는지 한 번 보기 — 답변 아래 「인용 S1 S3」 줄.
      > 막힌 것: 키가 사용자에게만 있다.
      **찾은 버그 1개**: 2B 모델이 인용을 `(S1, S5)` 로 써서 `[S1]` 만 찾는 추출기가 0개로
      셌다. 추출기를 표기에 관대하게 고쳤다 — 재려는 것은 「근거를 댔는가」이지
      「대괄호를 썼는가」가 아니다. 유효성은 판정이 따로 본다.
      **엔진 어댑터를 둘 다 지금 만들었다** (계획은 Ollama 를 S9 로 미뤘다): Gemini 를 검증할
      수 없으니 검증 가능한 엔진이 하나는 있어야 스트리밍·취소·인용을 실제로 확인할 수 있다.

- [~] **S6. 출처 공개 UI** — 출처 칩 → 근거 모달(조문 원문 + law.go.kr 링크 + 시행일자),
      검색 단계 표시, 법률 자문 아님 안내 상시.
      **시행 예정 조문은 현행과 시각적으로 구분.** 법령명을 줄이지 않는다(식품표시광고법 ≠ 표시광고법).
      *완료 기준*: A2(칩 클릭 → 실제 조문), A10(시행일 + **시행 예정 구분**), A11(법률 자문 아님)
      → **로컬·배포본 확인 완료.**
      **배포본 확인** (2026-08-29, 커밋 `02ca8c5`): 모델 다운로드 → 동일 공간 검증 통과
      (코사인 0.9992) → 검색 단계 6줄이 **로컬과 숫자까지 동일**(병합 49개 · 그중 11개 양쪽) →
      근거 줄 클릭 → 모달(`식품 등의 표시ㆍ광고에 관한 법률` · `제8조①제3호 · FLA-033` ·
      `시행 중 · 2025-09-19` · 「이 링크는 해당 조문으로 바로 갑니다」) → 닫기 후 상태까지
      비워짐(`len 0`). 콘솔 에러 없음. **질문 독립 처리 안내도 배포본에 있다.**
      **A2 통과** — 칩 `S2` 클릭 → 모달(줄이지 않은 법령명 「식품 등의 표시ㆍ광고에 관한 법률」·
      `제8조①제3호`·`FLA-033`·조문 원문·검색 경로 순위) → law.go.kr 링크를 실제로 따라가
      **제8조 제1항 제3호 「건강기능식품이 아닌 것을 …」 화면 도달 확인**. 근거 목록의 줄도
      같은 모달을 연다. **A11** 은 헤더·답변 아래·모달 셋 다에 있다.
      **A10 은 절반만 실행 확인** — 시행일은 칩·목록·모달에 다 나온다. 하지만 **코퍼스 365개가
      전부 시행 중**이라 「시행 예정」 배지를 화면에서 볼 수가 없다. 그래서 ① 근거 목록에
      「이 결과의 조문은 전부 오늘 기준 시행 중입니다」를 늘 적어 **배지가 안 보이는 것이
      「기능이 없어서」가 아님**을 화면에서 확인되게 하고 ② `isFutureEffective` 를 합성 청크
      4가지로 따로 검사했다(시행일 미래 / `inForce:false` / 오늘이 시행일 / 현행 — 전부 기대대로).
      **화면에서 주황 배지를 본 것은 아니다.**
      **검색 단계 6줄**을 실측 숫자로 드러냈다 — 질의 임베딩(ms) → 의미 후보 30 → 표기 후보 30
      → 병합(서로 다른 49개, 그중 11개는 양쪽) → 형제 무리 제한 → 상위 8개 적재.
      `hybridSearchTraced` 를 새로 뺐고 기존 `hybridSearch` 호출부(스크립트 3개)는 그대로다.
      **PRD 5절 규칙 5**(질문 독립 처리)를 질문 영역에 한 줄로 넣었다 — 여태 화면에 없었다.
      **375px 확인**: 가로 오버플로 없음, 모달 337px(여백 19px), 칩·단계 줄바꿈 정상.
      **인용 추출을 `src/lib/citations.ts` 로 뺐다** (인계 3-2 권고). S6·S7·S8 이 같은 값을
      봐야 한다. 추출 규칙은 **코퍼스를 세어서** 갈랐다 — FINDINGS 13절.
      **찾은 버그 2개**: ① 실측 답변이 `[S2][S5]` 와 `[4]` 를 **한 답변 안에서 섞어 썼다.**
      처음 만든 「S 표기가 하나라도 있으면 맨숫자 무시」 규칙이 `[4]` 를 흘렸다 — 첫 실행에서
      바로 드러났다 ② `dialog.close()` 로 `open` 은 `false` 가 되는데 **`close` 이벤트가 아예
      오지 않는다**(직접 붙인 리스너도 0회). 그 위에 상태 동기화를 세워 두었어서 닫아도 부모
      상태가 안 비워졌다. 닫는 길을 전부 `onClose` 로 모으고 Escape 는 `cancel` 로 받는다.
      **넷 다 실행 확인**(열기 / 닫기 버튼 / 같은 줄 다시 열기 / 바깥 클릭 / `cancel`).
      **미확인**: 배포본, **실제 키보드 Escape**(검증 브라우저의 합성 Escape 가 `<dialog>` 에
      닿지 않아 `cancel` 이벤트로만 확인했다 — 사람이 눌러 볼 항목), 「없는 자료 인용」 경고 칩의
      **화면 렌더**(모델이 없는 번호를 대는 답을 만들지 못했다. 추출·판정은 단위 검사로 확인)

- [~] **S7. 근거 상태 5단 + 범위 밖 안내** — 근거 충분 / 근거 약함 / **일부 범위 밖** /
      코퍼스 밖(법령 질문) / **규범 밖(법령 질문 아님)**. 임계값 2개 + 규칙 1개(인용 없으면
      "근거 충분" 아님).
      **다른 법령 이름은 사람이 검수한 고정 목록에서만 꺼낸다** — 모델이 대면 무근거 생성이고
      인용 규칙이 못 잡는다(PRD 4절).
      **근거 충분일 때도 개별 제품 해당 여부를 단정하지 않는다** — 조문 요건이 평가적이다.
      *완료 기준*: A4 재현(**최종 확정 세팅에서**). "대한민국 법령에 없다"는 뜻의 답이 없다.
      규범 밖 질문에 다른 법령 언급이 붙지 않는다. 임계값 수치를 여기 기록
      → **로컬·배포본 확인 완료** (LLM 판정층은 키가 없어 배포본 미확인).
      **배포본 확인** (2026-08-29): **키 없이** 두 거절이 다 뜬다 —
      화장품/주름 → `코퍼스밖` + 「화장품법 소관입니다」 + 「범위 밖이라는 것은 규정이 없다는
      뜻이 아닙니다」, 원가/수수료 → `규범밖` + **법령 이름 0건**(8개 낱말 검사).
      **둘 다 엔진 오류가 안 뜬다** = 엔진을 부르지 않았다는 뜻이고, 이게 이 판정을 검색
      단계에 둔 이유다. 콘솔 에러 없음.
      **🔴 PRD 3절 F4 의 전제가 실측에서 성립하지 않았다** — 「상태를 가르는 신호는 검색 최고
      점수의 두 임계값」인데, **밀집도 희소도 코퍼스 안/밖을 못 가른다.** Q5(일부범위밖)
      0.8473 과 Q7(코퍼스밖) 0.8474 가 0.0001 차이고, 「식당 위생등급은 어떻게 신고해서
      받나요」(코퍼스 밖)가 **Q1(근거충분)보다 밀집이 높다**(0.8738 > 0.8646). 근거충분/약함은
      더 나쁘다 — Q2(근거약함)가 아홉 문항 중 점수가 가장 높다. 상세는 FINDINGS 15절.
      → **그래서 임계값을 주 신호에서 「마지막 문지기」로 내렸다.** 실제 판별은 셋이 한다:
      ① 사람이 검수한 **범위 밖 주제 목록**(코퍼스 메타의 `outOfScope` 와 맞물린 키워드)
      ② **법령 질문 신호어**(코퍼스밖 ↔ 규범밖) ③ **코퍼스 사실** — 「별표 N과 같다」로 끝나는
      조문이 12개인데 **별표 본문은 0개**다. 그런 근거가 인용되면 근거충분이 아니고, 어느
      별표가 없는지를 한계 문장에 적는다. **이게 점수로는 절대 안 보이는 신호다.**
      **판정 정확도 13/14** (Q1~Q9 + 도메인 밖 프로브 5). `node scripts/probe-scope.mjs`.
      유일한 실패는 **Q2**(근거약함 기대 → 근거충분): 검색이 gold(`FLR-020`)를 못 가져와서
      별표 규칙이 발동할 기회 자체가 없다. **검색 실패와 판정 실패가 겹친 것**이고, 판정만
      고쳐서 될 일이 아니다 (S11b).
      **A4 재현 — 두 경로 다 화면에서 봤다.** ① 인용 규칙(모델이 인용을 하나도 안 달았다)
      ② 별표 규칙(`S1` 이 별표 2 를 가리키기만 한다 → 한계 문장에 「별표 2」가 그대로 찍힘).
      ①은 **임계값과 무관한 신호**라 PRD 8절 ‡ 가 권한 G2 후보로 그대로 쓸 수 있다.
      **G2 후보 확정**: 「소비자 안전을 위한 주의사항은 어떻게 표시해야 하나요?」 — 검색 1위가
      `FLR-020`(별표 2 를 가리키는 조문)이라 **별표 규칙이 구조적으로 걸린다.**
      **거절 두 종류 확인**: 「같은 스토어에서 화장품도 파는데 …「주름 개선」…」 → `코퍼스밖`,
      화면에 「화장품법 소관입니다」 + 「범위 밖이라는 것은 규정이 없다는 뜻이 아닙니다」.
      「쿠팡 수수료 빼면 … 원가를 어떻게 낮출 수 있을까요?」 → `규범밖`, **법령 이름이 하나도
      안 나온다**(화장품법·약사법·수출 등 9개 낱말 검사 0건). 둘 다 **엔진을 부르지 않았다.**
      범위 밖일 때 조문 목록은 지우지 않되 「근거」라고 부르지 않는다 — 흐리게 + 「이것들은 이
      질문의 근거가 아닙니다」.
      **찾은 버그 1개**: 별표 규칙을 처음엔 「인용이 **전부** 가리키기만 할 때」만 걸었더니,
      모델이 별표 조문을 포함해 6개를 인용하고 **답 안에 「별표의 내용이 명시되어 있지
      않습니다」라고 스스로 적었는데도** 상태가 「근거충분」으로 나왔다. 「하나라도」로 바꿨다.
      **미확인**: 배포본, Gemini 엔진에서의 판정, 임계값 ②의 안정성(관측에서 코사인 **0.859 vs
      0.86** 으로 판정이 뒤집혔다 — 칼날 위다)

- [~] **S8. 판정 — 규칙 배지 + LLM 배지** — 규칙(인용 유효성·거절) + LLM(근거성).
      스키마 강제 + 긍정형 필드명(FINDINGS 2절 계약). 판정 실패 시 답변 유지. 배지 모양 분리.
      *완료 기준*: A5. **조문에 없는 내용을 일부러 넣어 판정이 잡는지 확인** — FINDINGS 의
      미확인 항목을 닫는다. 못 잡으면 그 사실을 기록
      → **로컬 확인 완료. 배포본은 규칙 층만 확인** (LLM 층은 키가 없어 못 부른다).
      **배포본 확인** (2026-08-29): 거절 응답 두 건에서 **규칙 배지가 뜬다** —
      「✓ 거절 여부 — 정당한 거절」 / 「– 인용 유효성 — 해당 없음」, LLM 카드는 「해당 없음」.
      **A5 의 「모든 응답에 배지가 붙는다」가 배포본에서 성립한다.** LLM 층은 배포본에서
      Gemini 키가 있어야 하고 로컬 Ollama 는 배포본에서 막힌다(FINDINGS 9절) — **미확인**.
      **🟢 완료 기준의 핵심을 닫았다 — 일부러 넣은 거짓을 판정이 잡는다.**
      `node scripts/probe-judge.mjs --runs=3` — **같은 근거·같은 질문**에 답변만 바꿔 4개
      케이스를 3회씩. 없는 수치(과징금 3천만원·12포인트) · 없는 조문번호(식품위생법
      제31조의4) · **요건 뒤집기**(「질병 예방·치료 표시는 허용됩니다」)를 **전부 잡았다**
      (`만들어냄=true`, 점수 25). 대조군만 `false`·95. **3회 모두 값이 한 글자도 다르지
      않았다** (온도 0). 이걸로 FINDINGS 2절이 열어 둔 「오답을 제대로 잡는가」가 닫혔다.
      **④가 가장 중요하다** — 번호는 맞고 내용만 뒤집은 답은 **인용 규칙이 구조적으로 못
      잡는 것**(PRD F5 의 「못 잡는다」 칸)인데 LLM 층이 잡았다.
      **A5 두 경로 다 확인**: 답변에도, 범위 안내에도 배지가 붙는다. 거절일 때 규칙은
      「✓ 정당한 거절」, LLM 은 「해당 없음」이고 **엔진을 부르지 않는다**.
      **PRD 5절 규칙 4(모양 분리) 실측**: 규칙 칩 `border-radius: 0px` · LLM 알약 `999px`,
      카드 테두리 실선/점선. 계산된 스타일로 확인했다.
      **PRD 5절 규칙 3(판정 실패해도 답변 유지) 실측**: Ollama 로 답변을 받은 뒤 Gemini(키
      없음)로 바꿔 「다시 판정」 → `판정 실패` 알약. 그때 **답변 855자 · 근거 8개 · 출처 칩
      8개 · 근거 상태 배지가 전부 그대로**였다.
      **찾은 것 (버그는 아니고 판정의 한계)**: `citedIds` 필드가 **믿을 수 없다** — 답변에
      `[S1]` 이 분명히 있는데도 `false` 로 온다(②④). 모델이 「인용했는가」와 「제대로
      인용했는가」를 뒤섞는다. **그런데 상관없다** — 그 항목은 규칙 층이 결정적으로 재고
      있고, LLM 이 틀린 필드가 하필 규칙과 겹치는 필드였다는 것이 두 층으로 나눈 근거가 된다.
      화면에는 `groundedInSources`·`hallucinated`·점수만 띄운다.
      **점수는 사실상 2단계다** — 관측된 값이 95 아니면 25뿐이었다. 0~100 척도로 읽으면 안 된다.
      **미확인**: 배포본, **Gemini 엔진에서의 판정**(키 없음 — `responseSchema` 변환이
      실제로 통하는지 확인 못 했다. Ollama 경로만 확인됨), 판정 코멘트가 마크다운 머리말
      (`[판정 결과]`)을 달고 오는 것은 그대로 둔다(모델이 쓴 글을 고치지 않는다)

- [x] **S9. 엔진 전환 마무리** — **어댑터·선택 UI·자동 폴백 없음은 S5 에서 끝났다.**
      여기 남은 것: 연결 상태 표시, 콜드 43초 대응(예열 또는 진행 표시), README 의
      `OLLAMA_ORIGINS` 안내 확인.
      *완료 기준*: A9. **배포본에서** 로컬 Ollama 로 답이 나온다. 같은 질문을 두 엔진으로 돌려
      답변 리듬·판정 차이를 관측(확장 항목)
      → **완료. 다만 완료 기준의 절반은 「안 된다」로 닫혔다.**
      **A9 를 올렸다** — 「전환할 수 있다」에서 **「현재 엔진과 그 상태가 항상 보인다」**로.
      모델 받기 전 준비 화면에도 헤더에 배지가 뜬다(`엔진 — Gemini (API 키 필요) ○ 키 없음`).
      상태는 **정직한 단계만** 쓴다: Gemini 는 키 유무까지만(`키 있음 (미확인)`), 확인 안 된
      것을 「연결됨」으로 올리지 않는다. **틀린 키로 400 을 받아도 배지는 `미확인` 에 머문다.**
      Ollama 는 선택하면 **버튼 없이 자동 프로브**(20초 주기 + 답변 종료 직후).
      `/api/version` + `/api/tags` + `/api/ps` 셋을 한 번에 봐서 「연결 안 됨」·「모델 없음」·
      「콜드」·「예열됨」을 가른다.
      **콜드스타트는 예열과 진행 표시 둘 다** 넣었다 — 「미리 올려두기」로 빈 프롬프트
      로드(`keep_alive: 30m`), 첫 글자 대기 중 경과 초 + 43초 눈금 막대. 43초는 디스크 콜드
      1회 관측치이고 재예열은 4~7초라 **「예상이고 상한이 아니다」**를 화면과 README 에 적었다.
      **❌ 「배포본에서 로컬 Ollama 로 답이 나온다」는 안 된다 — 관문이 둘이다**
      (FINDINGS 9절). `OLLAMA_ORIGINS` 로는 못 넘는다. **제품은 요청 전에 경고하는 것으로
      대응한다** — 막히는 것을 미리 알려 주는 것도 유효한 완료 상태로 봤다.
      **찾은 버그 1개**: 프로브 effect deps 에 `busy` 를 넣었더니 선언 순서 때문에 TDZ —
      `ReferenceError: Cannot access 'busy' before initialization`. **실행해서 콘솔에서
      잡았다. 코드만 읽었으면 못 봤다.**
      **배포본 확인 완료** (2026-08-29): 준비 화면에서 헤더에
      `엔진 — Gemini (API 키 필요) ○ 키 없음` — **A9 의 「항상 보인다」가 배포본에서 성립**.
      그리고 배포본 origin 에서 `fetch('http://127.0.0.1:11434/api/version')` 와 `localhost`
      판 **둘 다 `TypeError: Failed to fetch`** — 같은 시각 curl 로는 서버가 살아 있고 그
      origin 에 403 을 준다. **서버가 죽은 게 아니라 브라우저가 막는다**가 재현됐다.
      **미확인**: Gemini `연결됨` 배지 실점등(유효 키가 에이전트에 없다), 차단 주체를 사설망
      제한으로 단정하지 못함(`ERR_BLOCKED_BY_CLIENT` 는 전용 코드가 아니고 관측이 에이전트
      조작 크롬 — **사람 크롬 재확인 항목으로 README 에 남겼다**). 배포본에서 **Ollama 를
      골랐을 때의 배지·예열 UI** 는 모델 다운로드(129MB)가 필요해 확인하지 않았다 —
      로컬 dev 에서는 확인했다

- [x] **S10. 사람 피드백** — 좋아요/싫어요, 브라우저 로컬, 자동 판정과 나란히.
      *완료 기준*: A6
      → **완료. 단 A6 는 S8 없이 완전히 닫을 수 없다.**
      👍/👎 를 `localStorage`(`m05.feedback.v1`)에만 둔다. 서버로 보내지 않는다.
      **판정과 나란히** — 인용 요약 줄 바로 아래 한 줄에 카드 둘. 왼쪽은 「자동 판정 /
      아직 없습니다 (S8)」 **점선 자리표시자**, 오른쪽이 사람 피드백. 실측으로 같은 줄
      확인(x=301 / x=637, 같은 y). 375px 에서는 세로 적층, 가로 오버플로 없음.
      **S8 은 왼쪽 카드를 실제 배지로 갈아 끼우면 되고 레이아웃을 다시 짤 필요가 없다.**
      저장 스키마: `{id, at, vote, question, answerText(4000자 캡), answerTruncated,
      cancelled, evidence[{label, chunkId, source, path}], cited[], invalidCited[],
      engine, model, flavor?, verdicts: null, v: 1}`. 정한 이유 넷 —
      ① **답변 전문을 저장한다**: 👎 만 남으면 왜 눌렀는지 복원할 수 없고 S11b 의 사람
      평가가 사후 재현 불가다 ② `evidence` 에 **청크 ID**: 라벨 `S1` 은 그 답변 안에서만
      유효하고 코퍼스가 갱신되면 추적이 끊긴다 ③ `cited`/`invalidCited` 를 **남길 때**
      계산: S8 이 나중에 붙어도 그때의 답변에 대한 값이어야 한다 ④ `verdicts: null` —
      「판정 안 함」과 「판정했는데 안 걸림」을 구분하려 빈 객체를 쓰지 않았다
      **A6 는 S8 뒤에 닫혔다 (2026-08-29).** `verdicts` 를 실제 판정 스냅숏으로 채웠고,
      👍 를 누르면 기록에 들어가 목록에 「판정 — 근거충분 · 규칙 인용 유효 · LLM 자료에
      근거함 · 95/100 (qwen3.5:2b)」로 보인다. **누른 순간 화면에 있던 값**을 저장한다.
      **미확인**: Gemini 엔진에서의 피드백(키 없음), 취소한 답변 평가, 저장 실패 화면,
      200개 상한·4000자 절단, **배포본**

- [x] **S11a. 평가 세트 확정 + 종단 하네스** `[병렬 가능]` — Q1~Q9 확정(판매채널에서 실제
      부딪힌 문구를 쓴다), 실패 원인 단계 기록.
      *완료 기준*: Q1~Q9 가 기계가 읽는 단일 출처에 있고 gold 가 조문 ID 다.
      하네스가 실패를 단계별로 귀속한다
      → **완료.** 아래 «평가 세트 Q1~Q9» 가 확정본. 기준선(top-k=8 · 희소 0.3 · mock 엔진):
      **검색 적중 4/6 · 프롬프트 적재 2/6**. Q1(검색) Q2(검색) Q3(프롬프트) Q5(프롬프트) 실패.
      Q4·Q6·Q7~Q9 는 S7·S8 이 없어 **미판정**. **LLM 을 한 번도 호출하지 않았다** —
      옆 워크스트림이 Ollama 를 독점하고 있었다. 실제 엔진 경로는 **미확인**

- [x] **S11b. 기준선 측정 + 3축 실험** — 기준선 측정 → 3축(top-k / 약한 근거 임계값 /
      인용 규칙 문구) 한 번에 하나만. **사람 스팟체크 3건**. **병렬 불가** (측정)
      *완료 기준*: 결과표 채워짐, D3·D4 확정. **임계값을 바꿨으면 A4 를 재확인한다**
      (G2 판정이 실험 축 2번을 따라 움직인다 — PRD 8절 ‡)
      **착수 전 주의 둘** — ① 하네스에 **단일축 가드**가 있다: 두 축 이상이 기준선에서
      움직이면 `exit 2`. 정말 같이 바꿔야 하면 `--allow-multi-axis` ② 하네스는 **검색
      단계와 프롬프트 단계를 분리해서 잰다.** 검색 = 하이브리드 병합 결과 전체 안에 정답이
      있는가(**top-k 축과 무관하게 고정**), 프롬프트 = 앱과 똑같은 순서로 실제 실렸는가.
      처음엔 후보 창을 `k×3` 으로 뒀는데 **축 1을 흔들면 검색 지표까지 같이 움직여 원인
      귀속이 불가능**했다. **이 분리를 깨지 마라.**
      → **완료 (2026-08-30). 결과는 아래 «S11b 결과표».** 실제 엔진(Ollama qwen3.5:2b)으로
      **12회** 돌렸다. 하네스를 S7·S8 실제 구현에 연결했고 **버그 3개**를 고쳤다
      (`labelled` 에 `hit` 누락으로 코사인이 늘 0 · 거절 문항까지 생성을 돌림 · 인용 유효
      분모가 인용 안 한 답변까지 셈).
      **🔴 가장 중요한 결과는 축이 아니라 잡음이다** (FINDINGS 17절). 아무것도 안 바꾸고
      세 번 돌리니 근거상태가 **7/9 · 7/9 · 6/9**, 인용을 하나도 안 단 문항이 **Q6 · 없음 ·
      Q3** 로 매번 달랐다. **셀당 1회로 잰 축 차이 대부분이 이 폭 안**이라 효과라고 말할 수
      없다. 반대로 **검색 4/6 · 프롬프트 2/6 은 12회 전부 동일** — 엔진이 안 끼는 구간이라
      단일 실행으로 읽어도 되는 지표는 이 둘뿐이다.
      **잡음대를 넘은 것 둘**: ① 인용 규칙 `loose` → 답변 6개 중 **4개가 인용 0개**
      (기준선 0~1개). 문구를 무르게 하면 2B 모델은 그냥 안 단다 ② `top-k=25` → 프롬프트
      적재 **3/6 (3회 모두, 결정적 이득)** 이지만 인용 0개 답변이 **3·3·4개**로 무너진다.
      **top-k 8 유지 결정** — 이 앱이 파는 건 답이 아니라 「어느 조문에서 나왔나」다.
      **A4 재확인 — 재현 불필요함을 확인했다.** 축 2(임계값)는 **하네스 인자로만** 흔들었고
      배포 코드의 `CITED_DENSE_MIN` 은 `0.86` 그대로다. 0.84·0.88 둘 다 잡음대 안이라
      **바꿀 근거가 없어 안 바꿨다.** G2 는 애초에 **별표 포인터 규칙**으로 걸리게 골라서
      임계값과 무관하다 (PRD 8절 ‡ 가 권한 형태). 다만 **칼날 위라는 것은 또 재현됐다** —
      기준선 #2 의 Q1 이 「코사인 **0.860 < 0.86**」으로 근거약함이 됐다.
      **사람 스팟체크 3건 — 2건이 기계와 어긋난다** (FINDINGS 18절). Q4: 답변이 `UNF-005`
      의 요건을 **거꾸로 요약**(「보존료를 **사용한 경우** 금지」 ← 조문은 「**못 쓰게 정한**
      보존료가 없다는 표시」가 금지)하고 「천연·자연」 조항을 「무첨가」에 적용했는데 **LLM
      판정이 95/100 · 근거함으로 통과**시켰다. Q2: **답변 산문은 「자료에 없습니다」라고
      맞게 말하는데 배지만 `근거충분`** — 유효 인용 3개가 조건을 채웠고 그 인용이 범위 밖
      고시(`ALG-*`)다. Q7 은 맞다(엔진 안 부름). **무효 인용은 12회 전부 0건.**
      **미확인**: Gemini 엔진에서의 축 반응(키 없음), 문항 9개·셀당 1회의 통계적 힘

- [~] **S12. 키 없는 방문자용 녹화 데모** *(범위 추가 — 빼도 제출 가능)*
      *완료 기준*: 키 없이 데모를 볼 수 있고 실시간 답변과 시각적으로 구분된다
      → **로컬 확인 완료. 배포본 미확인.** 녹화 3개 (`app/public/demo/`, 56KB):
      영양성분(`근거충분`) · 화장품(`코퍼스밖`, **엔진을 안 부른 응답**) · 주의사항(`근거약함`).
      `node scripts/record-demo.mjs <slug> "질문"` 이 실제 파이프라인을 돌려 **토큰 하나하나와
      그 도착 시각까지** 적고, 재생이 그 간격을 그대로 흘린다 (속도를 올리지 않는다 — 첫
      글자까지의 시간이 이 제품에서 실제로 아픈 지점이다).
      **재생이 화면을 베껴 그리지 않는다.** 넣는 것은 검색 결과·범위 판정·답변 글자뿐이고
      **근거 상태와 규칙 배지는 앱의 평소 코드가 다시 계산한다** — 그래야 데모가 「이렇게
      보였다」가 아니라 「이 코드가 이렇게 한다」의 증거가 된다. LLM 판정만 녹화본에서 꺼낸다.
      청크는 인덱스가 아니라 **ID** 로 적어서, 코퍼스가 바뀌면 재생을 **거절**한다.
      **확인한 것** (localhost, 2026-08-30): ① **임베딩 모델을 받지 않은 상태**에서
      (「근거 검색 준비」가 그대로 떠 있는 화면) 목록이 보이고 3개 다 재생됐다 — 키도
      모델도 없이 검색·근거·답변·판정 전부 화면에 선다 ② 시각 구분 4겹 — 스티키 배너 ·
      `data-demo="on"` · 구역 왼쪽 레일 **3px 파랑 vs 평소 1px** · 답변 머리의 「녹화」 칩
      ③ 재생 중 평가 버튼 `disabled` + 이유 문구, 「다시 판정」 버튼 숨김 (둘 다 엔진을
      부르거나 기록을 더럽힌다) ④ 「나가기」가 근거·상태·판정을 **전부** 치운다
      ⑤ 세 데모 연속 재생에서 **런타임 에러 0건**
      **찾은 버그 2개**: ① 녹화에 `sparse` 를 안 담았는데 `as HybridHit` 캐스트가 타입
      검사를 가려서, 화면이 `sparse.toFixed(2)` 에서 통째로 죽었다 — 캐스트를 지웠다
      ② LLM 판정 카드가 **현재 고른 엔진** 이름을 써서 재생 중 「Gemini · qwen3.5:2b」라는
      없는 조합이 떴다 — 녹화 당시 엔진을 쓰게 고쳤다
      **미확인**: 배포본, 실제 키보드 조작(스크립트로 눌렀다)


- [ ] **S13. 제출** — G1~G3 확정 후 배포본에서 **A1~A11** + **실패 조건 5개** 실행 확인. README 정리.
      *완료 기준*: 체크리스트 전부 실행 확인. **구글폼 제출은 사용자가 직접**

## 평가 문항 커버리지

| 평가 문항 | 슬라이스 |
|---|---|
| 1. 자기 도메인 기획 | S0 |
| 2. 자료·벡터스토어 | S2, S3 |
| 3. RAG 파이프라인 | S4, S5, S6, S7 |
| 4. 판정·실험 | S8, S10, S11a, S11b |
| 5. 배포·확장 | S1, S9, S12, S13 |

## 평가 세트 Q1~Q9 (S11a 확정)

단일 출처는 **`app/scripts/eval-set.mjs`** 다. gold 는 **조문 ID**이고, `verifyEvalSet()` 이
매 실행마다 ① ID 가 코퍼스에 있는지 ② **본문이 아직 의도한 조문인지**를 검사해 어긋나면 멈춘다.

| | 질문 (판매채널 문구) | gold 청크 ID | 기대 근거상태 |
|---|---|---|---|
| Q1 | 일반 가공식품인데 쿠팡 상세페이지에 "면역력 강화에 도움"이라고 적어도 되나요? | `FLA-031` | 근거충분 |
| Q2 | 수제 쿠키에 우유와 대두가 들어가는데 알레르기 유발물질을 어떻게 표시해야 하나요? | `LBL-037`, `FLR-020` | 근거약함 |
| Q3 | 과자류로 품목보고한 쿠키인데 포장에 영양성분표를 꼭 넣어야 하나요? | `LBL-037`, `FLA-023` | 근거충분 |
| Q4 | 썸네일에 "합성보존료 무첨가"라고 크게 강조해도 되나요? | `UNF-005`, `UNF-008`, `UNF-015` | 근거약함 |
| Q5 | 인스타그램에 협찬 보내고 올리는 리뷰도 이 법 적용을 받나요? 대가를 받았다는 표시도 해야 하나요? | `FLA-011`, `FLA-014` | 일부범위밖 |
| Q6 | 온라인 상세페이지 광고 문구도 올리기 전에 자율심의를 먼저 받아야 하나요? | `FLA-052`, `FLR-077~080` | 근거충분 |
| Q7 | 아마존 미국에 수출하려는데 영문 라벨에 뭘 넣어야 하나요? | 없음 (0건) | 코퍼스밖 |
| Q8 | 같은 스토어에서 화장품도 파는데 상세페이지에 "주름 개선"이라고 써도 되나요? | 없음 (0건) | 코퍼스밖 |
| Q9 | 쿠팡 수수료 빼면 남는 게 없어요. 이 제품 원가를 어떻게 낮출 수 있을까요? | 없음 (0건) | **규범밖** |

**5단이 전부 커버된다** — `verifyEvalSet` 이 빠진 칸이 있으면 문제로 보고한다.
표기 진단용 N1·N2 는 `PROBE_SET` 으로 분리했다(루브릭 지표 밖).

거절 기대 문항의 근거가 **정말로 0건인지** 확인한 기록:
- Q7 — 「수출」 키워드 5건은 전부 무관 (`UNF-014`·`LBL-023`·`LBL-025`·`LBL-029`·`GMO-005`)
- Q8 — 「화장품」 0건, 「주름」 0건
- Q9 — 「원가」·「마진」 0건. 「수수료」는 `FLA-059`(자율심의 수수료) 하나가 걸리는데 질문과
  무관하다 — **이 문항에서 검색이 근거를 「찾는」 것 자체가 위험 신호다**

## 슬라이스에서 확정할 수치·목록 (여기 채워 나간다)

| 항목 | 슬라이스 | 값 |
|---|---|---|
| D2 법령·고시 목록 | S2 | 법률 3종(제1~10/6/10조) + 고시 6종. `scripts/sources.mjs` |
| D2 청크 수 | S2·S3 | **365** (FLA 64 · FLD 20 · FLR 80 · UNF 18 · LBL 126 · ALG 11 · NUT 9 · GMO 24 · REV 13). 길이 중위 187자 · 최대 1156자 |
| **하이브리드 설정** | **S4** | 코드값 희소 0.3 · 후보 30 · 한글 2-gram · 형제 무리당 2 · top-k 8. ⚠ **재검토 필요** — 적중 근거였던 「7/8」이 부풀려진 값이었다 (FINDINGS 11절). S11b 에서 축 하나로 다시 봤다 → **8 유지**, 이유는 «S11b 결과표» |
| **임베딩 모델** | **S3** | **e5-small q8** (`Xenova/multilingual-e5-small`, 384차원, 실측 129MB). mean pooling + L2 정규화, 접두어 `query: ` / `passage: `. 단건 임베딩 |
| **평가 세트 단일 출처** | **S11a** | **`app/scripts/eval-set.mjs`** (gold = 조문 ID, 자기검사 `verifyEvalSet` 포함) |
| **종단 하네스** | **S11a** | **`app/scripts/eval-run.mjs`** (단계별 실패 귀속 + 단일축 가드) |
| **S11a 기준선** | **S11a** | top-k=8 · 희소 0.3 · **mock 엔진** → 검색 4/6 · 프롬프트 2/6 |
| **콜드스타트 상수** | **S9** | `OLLAMA_COLD_MS = 43_000` (디스크 콜드 1회 관측치. 재예열 4~7초) |
| **임계값 ① 희소 바닥** | **S7** | **희소(BM25) 상위5 합 `50`.** 이 아래면 코퍼스에 표기가 겹치는 조문이 사실상 없다. 실측: 도메인 밖 프로브 0~45.4, 코퍼스 안 최저 60.9(Q1) → 아래로 15 · 위로 11 여유 |
| **임계값 ② 인용 근거 코사인** | **S7** | **`0.86`.** 인용된 근거의 최고 코사인이 이보다 낮으면 근거충분 아님. **칼날 위다** — 관측에서 0.859 로 판정이 뒤집혔다 |
| **점수가 아닌 판별 신호** | **S7** | ① 범위 밖 주제 목록(`OUT_OF_SCOPE_KEYWORDS`) ② 법령 질문 신호어 ③ **별표 포인터 규칙**(가리키기만 하는 조문 12개 / 별표 본문 0개). **실제로 가르는 것은 이 셋이다.** ③은 2026-08-30 에 **「인용된 것」→「실린 것」으로 넓혔다** (FINDINGS 19·19-1절) — A4 가 모델의 변덕에서 풀렸고, 영향은 12문항 중 G2 하나뿐이라 기준선은 안 움직였다 |
| **S7 판정 정확도** | **S7** | **13/14** (Q1~Q9 + 도메인 밖 5). 실패는 Q2 하나 — 검색이 gold 를 못 가져와 규칙이 발동할 기회가 없다 |
| **S8 판정 스키마** | **S8** | `JUDGE_SCHEMA` — `groundedInSources` · `hallucinated` · `citedIds` · `refusedForNoEvidence` · `scoreOutOf100` · `comment`. 온도 0, 스키마 강제 |
| **S8 거짓 탐지** | **S8** | 없는 수치 · 없는 조문번호 · **요건 뒤집기** 3종을 3회씩 **전부 탐지**, 3회 값 동일. `node scripts/probe-judge.mjs --runs=3` |
| **S8 알려진 한계** | **S8** | `citedIds` 는 믿을 수 없어 **화면에 안 띄운다**. 점수는 사실상 **95/25 두 값**뿐이라 0~100 척도가 아니다 |
| **top-k** | **S11b** | **8 유지** (확정). `k=25` 는 프롬프트 적재를 3회 모두 2/6→3/6 으로 올리지만 인용 0개 답변이 3·3·4건으로 무너진다 (기준선 0~1건). `k=15` 는 잡음대 안 |
| **D3 통과선 수치** | **S11b** | **결정적 지표에만 통과선을 둔다** — ① 검색 적중 **≥ 4/6** ② 프롬프트 적재 **≥ 2/6** ③ **무효 인용 0건** (12회 전부 0건이라 실제로 지켜지는 선이다). 근거상태는 **6~7/9 잡음대**로 적고 통과선을 두지 않는다. LLM 근거성은 **단일 실행으로 못 잰다** (2/5~5/6) |
| **D4 루브릭 기준선** | **S11b** | **기준선 = ollama qwen3.5:2b · top-k 8 · 희소 0.3 · 0.86 · 인용 default → 검색 4/6 · 프롬프트 2/6 · 무효 인용 0 · 근거상태 6~7/9 · 인용 0개 0~1건.** 축 비교는 **이 폭 밖일 때만** 효과로 센다 (FINDINGS 17절) |
| **D8 G1~G3 질문** | **S13** | **확정.** G1 「부당한 표시·광고를 하면 어떤 처벌을 받나요?」(조문 링크 **7/8 정확** — A2 가 어느 칩을 눌러도 걸린다) · G2 「소비자 안전을 위한 주의사항은 어떻게 표시해야 하나요?」(포인터 규칙으로 **결정적**) · G3 「막걸리 라벨에 도수랑 원재료를 어떻게 적어야 하나요?」(→ `코퍼스밖`, 주세법 안내). **평가 세트 Q1~Q9 와 겹치지 않는다** |
| 시행 중인 판 취득 경로 | S2 | _미정_ |
| 다른 법령 고정 안내 목록 | S7 | **확정 7개** — 코퍼스 메타 `outOfScope` 가 이름의 단일 출처이고, 알아보는 키워드는 `src/lib/evidence-state.ts` 의 `OUT_OF_SCOPE_KEYWORDS`. 수출 라벨 / 화장품 / 의약품 / 건기식 **기능성 표시** / 축산물 / 주류 / 협찬·대가관계 |
| **G2 (경고 배지 재현 질문)** | **S7** | 「소비자 안전을 위한 주의사항은 어떻게 표시해야 하나요?」 — 검색 1위가 `FLR-020`(별표 2 를 가리키기만 하는 조문)이라 **임계값과 무관하게** 근거약함이 뜬다 (PRD 8절 ‡ 가 권한 형태) |

## S11b 결과표

전부 `--engine=ollama` (qwen3.5:2b, 온도 0) · 문항 9개(생성 대상 6개 + 거절 3개) · **셀당 1회**
(기준선과 `k=25` 만 3회). `node scripts/eval-run.mjs --engine=ollama [축] --label=...`

| 실행 | 축 | 검색 | 프롬프트 | 무효 인용 | 근거상태 | 인용 0개 답변 |
|---|---|---|---|---|---|---|
| 기준선 #1 | — | 4/6 | 2/6 | 0 | 7/9 | Q6 |
| 기준선 #2 | — | 4/6 | 2/6 | 0 | 7/9 | — |
| 기준선 #3 | — | 4/6 | 2/6 | 0 | 6/9 | Q3 |
| k15 | top-k 8→15 | 4/6 | 2/6 | 0 | 7/9 | Q5 |
| k25 #1 | top-k 8→25 | 4/6 | **3/6** | 0 | 5/9 | Q1 Q3 Q6 |
| k25 #2 | top-k 8→25 | 4/6 | **3/6** | 0 | 5/9 | Q3 Q4 Q5 |
| k25 #3 | top-k 8→25 | 4/6 | **3/6** | 0 | 6/9 | Q1 Q2 Q3 Q6 |
| w0.84 | 임계값② 0.86→0.84 | 4/6 | 2/6 | 0 | 7/9 | — |
| w0.88 | 임계값② 0.86→0.88 | 4/6 | 2/6 | 0 | 6/9 | Q3 Q5 |
| strict | 인용 규칙 문구 | 4/6 | 2/6 | 0 | 6/9 | Q6 |
| loose | 인용 규칙 문구 | 4/6 | 2/6 | 0 | 7/9 | **Q1 Q2 Q5 Q6** |

**읽는 법 — 잡음대(기준선 3회) 밖만 효과로 센다.** 근거상태 6~7/9, 인용 0개 0~1건이
잡음대다. 넘은 것은 굵게 표시한 둘뿐이다: `k=25` 의 프롬프트 +1(3회 재현, 결정적 지표)과
`loose` 의 인용 붕괴(4건). `k15` · `w0.84` · `w0.88` · `strict` 는 **효과 없음이 아니라
「이 설계로는 측정 못 함」**이다 — 문항을 늘리거나 셀당 반복을 늘려야 한다.

**바꾼 것: 없다.** top-k 8 · 희소 0.3 · 임계값 0.86 · 인용 규칙 `default` 를 유지한다.
`k=25` 만 유일하게 이득이 확실했는데, 같은 크기로 확실한 손해(인용 붕괴)가 붙어 있다.


## 검증 원칙

- **각 슬라이스마다 배포본에서 확인한다.** 로컬만 되는 것은 완료가 아니다
- 수용 기준은 **G1~G3 전용 질문**으로 확인. 평가 세트 9개를 게이트로 쓰지 않는다
- "된다"는 실행해서 본 것만. 확인 못 한 것은 "미확인"으로 남긴다
