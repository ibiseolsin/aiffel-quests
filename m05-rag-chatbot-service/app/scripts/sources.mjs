/**
 * 코퍼스 대상 자료 목록.
 *
 * 범위: "내가 만든 식품을 판매채널에 올릴 때, 표시사항과 광고 문구가 규정에 맞는가"
 * 대한민국 법령 전체가 아니다 — 이 목록이 곧 챗봇의 범위이고, 밖은 "범위 밖"으로 처리한다.
 *
 * code: 청크 ID 접두어. 법령을 추가해도 기존 ID 가 밀리지 않도록 자료별로 따로 센다.
 * maxArticle: 여기까지만 수집한다. 범위를 먼저 정하고 그 안에서 사실 단위로 나눈다 (PRD 4절).
 *   제재·과징금·벌칙·기구 등록 같은 집행 조문은 뺀다 — 사용자의 질문("이 문구 써도 되나")에
 *   답하는 근거가 아니고, 넣으면 개별 제품의 처벌 수준을 단정하게 된다 (PRD 6절 비목표).
 * shortName: 화면 출처 칩에 쓰는 이름. 줄이지 않는다 —
 *   「표시광고법」은 공정거래위원회 소관 「표시·광고의 공정화에 관한 법률」과 구분되지 않는다.
 */

/**
 * 법령은 `법령ID` 로 고정한다. `MST`(법령일련번호)는 개정마다 바뀌므로 키로 쓰지 않는다.
 * 본문은 `target=eflaw&ID=<법령ID>` 로 받는다 — 이게 정의상 "오늘 시행 중인 본문"이다.
 * `target=law` 는 쓰지 않는다: `기본정보.시행일자` 가 그 공포분의 부칙 시행일이라 오늘과 무관하다.
 * (실측: 시행규칙을 target=law 로 받으면 2024-12-30 판, eflaw 로 받으면 2026-01-01 판)
 */
export const LAWS = [
  {
    code: 'FLA',
    lawId: '013094',
    shortName: '식품표시광고법',
    kind: '법률',
    // 제1~10조: 목적·정의·표시기준·영양표시·나트륨·광고기준·부당광고금지·실증·자율심의
    // 제11조 이후: 심의위원회 조직 · 시정명령 · 영업정지 · 과징금 · 벌칙 → 범위 밖
    maxArticle: 10,
  },
  {
    code: 'FLD',
    lawId: '013453',
    shortName: '식품표시광고법 시행령',
    kind: '시행령',
    // 제1~6조: 금지 대상 · 부당광고 내용 · 심의 기준 · 이의신청
    // 제7조 이후: 교육 위탁 · 과징금 산정 · 기금 · 공표 · 과태료 → 범위 밖
    maxArticle: 6,
  },
  {
    code: 'FLR',
    lawId: '013475',
    shortName: '식품표시광고법 시행규칙',
    kind: '시행규칙',
    // 제1~10조: 표시사항 · 표시의무자 · 표시방법 · 영양표시 · 나트륨 · 광고기준 · 실증 · 심의대상
    //   이 코퍼스에서 "무엇을 어떻게 적어야 하는가"에 가장 직접적으로 답하는 자료다
    // 제11조 이후: 수수료 · 기구 등록 · 회수 · 행정처분 기준 → 범위 밖
    maxArticle: 10,
  },
]

/**
 * 행정규칙(고시)은 **시행일 기준 본문 조회 API 가 없다.**
 *
 * `현행여부=Y` 는 "가장 최근 발령"을 뜻할 뿐 시행 여부와 무관하다 — 실측으로 「식품등의
 * 표시기준」의 `현행` 판이 시행 2028-01-01 이었다. 그래서 판 목록을 받아 직접 고른다:
 *
 *   시행일자 <= 오늘 인 판 중 **발령일자가 가장 늦은 것**
 *
 * `max(시행일자)` 로 고르면 안 된다. 표시기준에서 그러면 발령 2025-04-21 판이 뽑히는데
 * 그 본문에는 2025-08 개정이 아예 없다 (판은 발령일자 순으로 누적된다).
 *
 * ruleId: 판을 묶는 키(`행정규칙ID`). 개별 판은 `행정규칙일련번호` 로 가리킨다.
 */
export const ADMRULS = [
  {
    code: 'UNF',
    ruleId: '69549',
    searchName: '식품등의 부당한 표시 또는 광고의 내용 기준',
    shortName: '부당한 표시·광고 내용 기준',
    kind: '고시',
    note: '금지되는 표현의 구체 예시. 이 코퍼스에서 가장 실용적인 자료',
  },
  {
    code: 'LBL',
    ruleId: '36814',
    searchName: '식품등의 표시기준',
    shortName: '식품등의 표시기준',
    kind: '고시',
    note: '의무 표시사항. API 의 현행 판은 시행 2028-01-01 이라 쓸 수 없다 — 판 목록에서 골라야 한다',
  },
  {
    code: 'ALG',
    ruleId: '56481',
    searchName: '어린이 기호식품 등의 알레르기 유발 식품 표시기준 및 방법',
    shortName: '어린이 기호식품 알레르기 표시기준',
    kind: '고시',
    note: '대상이 어린이 기호식품에 한정된다. 일반식품 알레르기 표시의 근거가 아니다',
  },
  {
    code: 'NUT',
    ruleId: '37092',
    searchName: '어린이 기호식품 등의 영양성분과 고카페인 함유 식품 표시기준 및 방법에 관한 규정',
    shortName: '어린이 기호식품 영양성분·고카페인 표시기준',
    kind: '고시',
  },
  {
    code: 'GMO',
    ruleId: '44603',
    searchName: '유전자변형식품등의 표시기준',
    shortName: '유전자변형식품등의 표시기준',
    kind: '고시',
    note: 'API 의 현행 판은 시행 2026-12-31 이라 쓸 수 없다',
  },
  {
    code: 'REV',
    ruleId: '66910',
    searchName: '식품등의 표시 또는 광고 심의 및 이의신청 기준',
    shortName: '표시·광고 심의 및 이의신청 기준',
    kind: '고시',
  },
]

/** 이 챗봇의 범위 밖. 다른 법령 이름을 댈 때는 이 목록에서만 꺼낸다 (PRD 4절). */
export const OUT_OF_SCOPE = [
  { topic: '수출 라벨 요건', owner: '수입국 규정 및 수출 관련 법령' },
  { topic: '화장품 표시·광고', owner: '화장품법' },
  { topic: '의약품 표시·광고', owner: '약사법' },
  { topic: '건강기능식품의 기능성 표시 허용 범위', owner: '건강기능식품에 관한 법률' },
  { topic: '축산물 개별 표시기준', owner: '축산물 위생관리법' },
  { topic: '주류 표시', owner: '주세법 및 주류 관련 고시' },
  { topic: '협찬·대가관계 표시 등 일반 부당광고', owner: '표시·광고의 공정화에 관한 법률 (공정거래위원회 소관)' },
]
