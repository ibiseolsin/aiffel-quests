# PLAN — agent m02 하네스 개선 실험 보고서

> 이 파일도 `PRD.md` 와 같이 실행 뒤에 규약을 맞추려고 썼다. 슬라이스는 **실제로 지나온
> 순서** 그대로 적었고, 체크는 실행으로 확인된 것에만 했다.

## 1단계 — 설계와 실행 (완료, 2026-09-03 ~ 09-07, `7469659`..)

- [x] **S1. 원문 요구사항 정리와 계측 가능성 확인** — 강의노트 11강·과제 절을 읽고
      `claude -p --output-format json` 이 `usage`·`num_turns`·`permission_denials`·
      `duration_ms` 를 내는지 실호출로 확인.
      완료 기준: 네 지표가 실제 출력에 있다. **확인함** (프로브 1회).

- [x] **S2. 불편 장면을 1차 기록에서 확정** — 후보 셋 중 사용자가 컨텍스트 관리를 선택.
      m05 의 `PLAN.md:464` 「다음 세션이 잘못 판단하기 쉬운 것 셋」과 FINDINGS 9절 반전
      커밋을 근거로 고정.
      완료 기준: 과업·입력·오류가 커밋/줄번호로 지목된다. **확인함**.

- [x] **S3. fixture 고정** — 문서 두 벌(full 21,103 B / folded 3,769 B)과 과업 3개,
      보류 채점 테스트. `build.py` 가 현재 절 동일성·옛 규칙 유무·새 규칙 존재 3종을 검사.
      완료 기준: 검사 3종 통과, 스텁 상태 전 fixture 실패, 테스트 유출 0. **확인함**.

- [x] **S4. 사전 판정식 확정 후 커밋** — `experiment-01.md` 6절.
      완료 기준: 실행보다 앞선 커밋이 존재한다. **확인함** (`7469659`).

- [x] **S5. 파일럿과 계측 결함 수정** — 첫 파일럿 2셀이 `max_turns` 로 종료. 원인은
      `Bash` 미허용으로 인한 검증 거부 루프. `Bash` 허용 + `--max-turns` 30 으로 수정,
      두 조건에 동일 적용. 셀당 정가 환산 $0.438 → $0.146.
      완료 기준: `terminal_reason: completed`, 거부 0. **확인함** (pilot2).

- [x] **S6. 본 실행 12셀** — 3 fixture × 2 조건 × 2 반복, 고정 시드로 순서 섞음.
      완료 기준: 12셀 전부 정상 종료, `results.csv` 생성. **확인함** (`is_error` 0).

- [x] **S7. 판정과 보고서** — 사전 판정식 대입 결과 **기각**. 궤적 분석으로 「못 본 게 아니라
      물리쳤다」 확인. 조작 전달 검증(문자수 차 9,266 vs 토큰 차 9,139, 비 1.01).
      완료 기준: README 에 가설→설계→결과→재검토 조건 순서와 맺음 5칸이 있다. **확인함**.

- [x] **S8. 리포 규약 파일 정리** — `PRD.md`·`PLAN.md` 작성, 루트 README 표 갱신.
      완료 기준: 리포 `CLAUDE.md` 가 요구하는 파일이 다 있다. **확인함**.

## 남은 것

- [ ] **S9. LMS 「프로젝트 제출」에 저장소 URL 제출** — **사용자가 직접.** 에이전트가 하지 않는다.

## 다음 단계 (착수 전, 제목과 완료 기준만)

- **2단계 — experiment-02: 폐기 표기 유무 조작.** 길이를 양쪽 21KB 로 고정하고 완료 절의
  옛 결정에 「폐기」 표기가 있는가 없는가만 바꾼다.
  완료 기준: 사전 판정식을 실행 전에 커밋하고, 12셀을 돌려 길이와 옛 문장의 기여를 분해한다.

## 이 실험에서 얻은 것 중 다음 세션이 잘못 읽기 쉬운 것

1. **「12/12 통과」를 「접기가 쓸모없다」로 읽지 마라.** 토큰은 실제로 10.5% 줄었다.
   기각된 것은 **정확도 주장**이지 접기 규칙 자체가 아니다.
2. **`permission_denials: 0` 을 「승인이 필요 없었다」로 읽지 마라.** 헤드리스에 승인 창이
   없다는 뜻이다. 이 실험은 승인 지표를 **재지 못했다**.
3. **파일럿 4셀은 판정 데이터가 아니다.** 계측 교정용으로 실행 전에 선언했고, 첫 2셀은
   설정 결함으로 오염돼 있다.
