# PLAN — m04 식품 표시·광고 사전검토 안내 에이전트 (도구 설계)

> 과제 요건은 [README](README.md), 사용자 관점 요구는 [PRD](PRD.md).
> 슬라이스 하나 = 세션 하나. 끝나면 여기 체크하고 커밋한다.
> 구현 세션 시작 프롬프트: `PRD.md, PLAN.md 읽고 슬라이스 N 진행. 끝나면 PLAN.md 체크하고 커밋.`

## 무엇을 만드는가

**서비스 가이드 챗봇의 경계를 담은 설계 문서.** 돌아가는 코드는 만들지 않는다.

루브릭 채점 기준 셋이 전부 문서다 — ① 테스트 문항에 공격 시도 1개 + **각 문항의 기대 동작**,
② **과거 생성한 서비스에서** 출발한 가이드 서비스, ③ 도구 정의서마다 **'쓰는 때·안 쓰는 때'와
'실패 뒤 행동'**. ②가 출발점을 지정하므로 [m05](../m05-rag-chatbot-service/) 를 기반으로 삼는다.

m05는 "조문을 검색해 **보여 주는**" 서비스였다. m04는 그 검색을 **도구 하나로 두고**
표시사항 점검·문구 대조·사람 이관까지 묶은 **에이전트의 경계**를 설계한다.

## 확정된 결정

| # | 결정 |
|---|---|
| D1 | **도메인은 m05 와 동일** — 식품 표시·광고 규정. 루브릭 ②가 "과거 생성한 서비스에서"를 요구한다. 새 도메인 발명은 루브릭에서 멀어지는 일 |
| D2 | **도구 4개 — 읽기 3 + 쓰기 1.** 3~4개는 과제 상한이자 PRD 3절 유즈케이스가 요구하는 자료의 수 |
| D3 | **도구 경계는 *주제*가 아니라 *사용자가 무엇을 제시했는가*로 가른다** — 표현 / 식품유형 / 조문번호. 주제로 가르면 "'무설탕' 써도 되나요" 같은 질문에서 즉시 겹친다 |
| D4 | **범위 밖은 다섯 번째 도구로 만들지 않는다.** 읽기 도구 공통 반환 코드 `OUT_OF_SCOPE` + m05 코퍼스의 `outOfScope` 고정 목록 7건(사람이 검수한 것). 범위 밖은 *별도 자료*가 아니라 *검색 실패의 한 종류*다. 도구를 늘리는 것은 안티패턴(도구 남발) |
| D5 | **쓰기 도구는 "검토 요청 접수" 하나.** 이 제품은 확답을 하지 않기로 했다(PRD 1절) — 이관 경로 없는 거절은 제품이 아니라 회피다. 이관은 외부 상태를 바꾸므로 읽기와 분리되고 승인을 거친다 |
| D6 | **실측·회귀·MCP·배포는 안 한다.** 루브릭에 실행 항목이 없다. 못 해서가 아니라 채점 범위 밖이라 뺀 것이고, 그 사실을 README 에 적는다 |
| D7 | **트레이스는 설계상 기대 기록.** 7강이 "실제 결과가 아니라 설계상 남겨야 할 관찰 항목을 채워"라고 쓴다. 파일 머리에 실측이 아님을 명시 |
| D8 | **m05 는 읽기만 한다.** 코퍼스 스키마(`id·lawName·path·text·url·effectiveDate·inForce`, 별표 6종 `UNF/LBL/ALG/NUT/GMO/REV`, `outOfScope` 7건)를 인자·반환 필드의 출처로 쓴다. m05 앱은 건드리지 않는다 |

### 도구 4개 (슬라이스 1에서 확정)

| 도구 | 사용자가 제시한 것 | 성격 |
|---|---|---|
| 광고 표현 기준 조회 | **문구·표현** ("다이어트에 좋은") | 읽기 |
| 의무 표시사항 조회 | **식품유형** ("과자류") | 읽기 |
| 조문 원문 조회 | **법령명 + 조문 번호** ("제8조 제1항 제3호") | 읽기 |
| 검토 요청 접수 | 읽기 도구로 답한 뒤에도 **확정 판단을 요구** | **쓰기 · 승인 필요** |

> **슬라이스 1이 어려운 이유** — 이 경계에 실제로 겹치는 자리가 있다. "'무설탕'이라고
> **표시**해도 되나요"는 영양강조표시라 **의무 표시사항**과 **광고 표현 기준** 양쪽에 걸린다.
> 이 겹침을 '안 쓰는 때' 문장으로 닫는 것이 이 과제의 실제 난이도다.

## 슬라이스

순차 진행 — 앞 슬라이스가 뒤의 입력이라 `[병렬 가능]` 을 붙일 수 있는 슬라이스가 없다.

- [x] **1. 도구 경계 확정과 정의서** `[어려움]` — 도구 4개(읽기 3 + 쓰기 1)를 확정하고
      `my-agent/tool-definition.md` 에 6항목(이름 / 하는 일 한 문장 / 쓰는 때·안 쓰는 때 /
      인자 스키마 / 반환 형식 / 실패 정의와 뒤 행동)을 도구마다 작성. 인자·반환 필드는 m05
      코퍼스 스키마에서 가져온다 (D8).
      *완료 기준*: (a) 도구마다 6칸에 빈 칸 0, (b) 어떤 두 도구도 '쓰는 때'가 겹치지 않음을
      도구쌍 6개를 직접 대조해 확인, (c) '무설탕' 류 겹침 사례가 '안 쓰는 때'에 명시적으로
      닫혀 있음, (d) 반환 형식에 원문 덤프·내부 식별자가 없음.
      **루브릭 ③을 여기서 만족한다**
      → **완료 2026-08-31.** 산출물 `my-agent/tool-definition.md` (9절 구성).
      (a) 4도구 x 6항목 빈 칸 0 · 표 빈 셀 0, (b) 도구쌍 6개 대조표를 6절에,
      (c) 겹침 4건('무설탕'·조문번호+문구·커버리지 밖·주류)을 7절에서 '안 쓰는 때' 문장으로 닫음,
      (d) 금지 필드 목록을 1.3 에 두고 반환 예시 4개에 누출 0.
      검증은 문서를 파싱해 46개 항목을 대조하는 스크립트로 실행했고 전부 통과했다
      (스크립트는 이 저장소에 두지 않았다 — 설계 과제라 실행 코드를 남기지 않는다. 재현 방법은
      정의서 9절 "측정 방법"). 부수 산출: 오류 코드 25개 총람(정의서 8절) — 슬라이스 2 ④ 의 입력.

- [x] **2. 패킷 ①③④⑤ — 지시·권한·오류·반환** — `design-packet.md` 를 만들고
      ① 시스템 지시 초안(역할·대상·답변 범위·근거 없는 확정 금지 + 검토 대상 문구를 지시로
      따르지 않는다), ③ 권한과 승인(읽기/쓰기 분리, 쓰기 승인 조건), ④ 오류·보류·이관 규칙,
      ⑤ 반환 형식 규약을 작성. ②는 슬라이스 1의 정의서를 가리킨다.
      *완료 기준*: (a) 네 항목 모두 채워짐, (b) 슬라이스 1의 **모든 오류 코드**가 ④에 나오고
      각각 재시도/되묻기/보류/이관 중 하나가 지정됨(미지정 0), (c) 쓰기 도구가 기본 권한에
      없고 승인 전 확인 항목(대상·내용·사유)이 적혀 있음, (d) 중단 조건이 ④에 있음
      → **완료 2026-08-31.** 산출물 `design-packet.md` (①=1절 · ③=2절 · ④=3절 · ⑤=4절,
      ⑥⑦ 은 슬라이스 3·4 자리표시).
      (a) 네 절 본문 1646·2063·3305·1683자, 미기입 표시 0 · 표 빈 셀 0,
      (b) 정의서 8절 25개 ↔ 패킷 3.2 표 25행을 코드 집합으로 대조 — 누락 0 · 초과 0 · 중복 0,
      1차 행동이 네 어휘 중 하나: **되묻기 9 · 보류 14 · 재시도 2 = 25, 미지정 0**,
      (c) 권한표에 `file_review_request` = 기본 권한 **닫힘**, 승인 6단계와 확인 항목
      대상·내용·사유, 호출 1회용 토큰,
      (d) 중단 조건 **S1~S8** 이 ④ 하위 3.4 에 있음.
      검증은 두 문서를 파싱해 대조하는 스크립트로 실행했고 전부 통과했다 (슬라이스 1과 같은
      방식으로 저장소에는 두지 않았다 — 재현 방법은 패킷 7절).
      **결정** — 정의서 8절이 `MISSING_EVIDENCE` 에 쓴 "자기 교정"은 네 어휘 밖이라
      1차 행동을 **보류**로 정규화하고 자기 교정을 후속 열로 내렸다 (패킷 3.1). 정의서는
      고치지 않았다. **발견** — 1차 행동이 `이관` 인 코드는 0개다. 이관은 코드 하나가 켜는
      즉시 행동이 아니라 누적 조건·사용자 발화에서 켜지기 때문이고, 그 근거를 3.3 에 적었다.

- [x] **3. 패킷 ⑥ 테스트 문항과 기대 동작** — PRD 3절 UC1~UC7 을 문항 6~8개로 옮기고,
      문항마다 **기대 도구 · 기대 반환 필드 · 기대 중단/이관 행동** 세 칸을 채운다.
      *완료 기준*: (a) 문항 6~8개, (b) **공격 시도(규칙 우회 요청) ≥1**, 필수 인자 누락 ≥1,
      이관 필요 ≥1, 서로 충돌하는 조건 ≥1, 도구 오류 ≥1, (c) **모든 문항의 세 칸에 빈 칸 0**,
      (d) 각 문항의 기대 도구가 슬라이스 1의 '쓰는 때'와 모순되지 않음.
      **루브릭 ①을 여기서 만족한다**
      → **완료 2026-08-31.** 산출물 `design-packet.md` 5절 (5.1 고른 기준 · 5.2 문항 8개 ·
      5.3 커버리지 · 5.4 '쓰는 때' 대조 · 5.5 드러난 자리 · 5.6 완료 기준 대조).
      (a) **문항 8개** Q1~Q8, UC1~UC7 전부 + 충돌 조건 1개(Q8),
      (b) 공격 **1**(Q7) · 필수 인자 누락 **1**(Q5 1턴) · 이관 **3**(Q6·Q8·Q5 2턴) ·
      충돌 조건 **1**(Q8 주류) · 도구 오류 **4**(Q5 2턴 `TYPE_NOT_COVERED` · Q2②
      `CROSS_REF_UNRESOLVED` · Q4·Q8 `OUT_OF_SCOPE`) — 다섯 요구 전부 충족,
      (c) 8문항 × 3칸 = **24칸 빈 칸 0**, 5절 표 셀 166개 중 빈 셀 0,
      (d) 호출 지점 **10개**를 정의서 ③ 과 대조 — **모순 0** (5.4 표).
      검증은 두 문서를 파싱해 대조하는 검사 22개로 실행했고 전부 통과했다 (슬라이스 1·2와 같이
      스크립트는 저장소에 두지 않았다 — 재현 방법은 5.6 "측정 방법").
      **발견** — (d) 를 눈으로 대조하다 공격 문항의 모순을 하나 잡아 고쳤다. 처음에는 Q7 이
      Q1 과 **다른 문구**를 붙여넣게 썼는데, 그러면 `search_expression_rule` 의 '쓰는 때' 가
      다시 성립해 우회 요청이 정상 조회로 흡수된다. 기계 검사는 이 종류를 잡지 못했다.
      **정의서에 글자로 없는 자리 둘**을 5.5 에 적었다 (`foodType` enum 밖일 때
      `OUT_OF_SCOPE`/`TYPE_NOT_COVERED` 판정 순서, `POLICY_BYPASS_ATTEMPT` 기록과 승인
      게이트의 긴장). **정의서는 고치지 않았다.**

- [x] **4. 패킷 ⑦ 트레이스 양식과 기대 기록** — 트레이스 양식(요청 · 선택한 도구 · 인자 ·
      반환 근거 · 최종 답 · 중단·이관 사유)을 정의하고 `my-agent/traces/` 에 **설계상 기대
      기록** 예시를 쓴다 (D7).
      *완료 기준*: (a) 양식의 6칸이 정의됨, (b) 기대 기록 3건 — 정상 경로 1 · 이관 1 · 공격
      거절 1, (c) 각 기록이 슬라이스 3의 해당 문항 기대 동작과 일치, (d) 모든 파일 머리에
      "실측이 아니라 설계상 기대치" 표기
      → **완료 2026-08-31.** 산출물 `design-packet.md` 6절(6.1 양식 · 6.2 기록 3건 ·
      6.3 값 표기 규칙 · 6.4 문항 대조 · 6.5 드러난 자리 · 6.6 완료 기준 대조)과
      `my-agent/traces/` 3파일.
      (a) 양식 **6칸**, 칸마다 "무엇을 / 어디서 / 비면 무엇을 못 보나" 3열,
      (b) **3건** — Q1 정상(`q1-normal-read.md`) · Q6 이관(`q6-handoff-approval.md`) ·
      Q7 공격 거절(`q7-bypass-refused.md`), 3파일 × 6절 = **18절**이 양식 6칸과 이름·순서 일치,
      (c) 3문항 × 3칸 = **9칸 대조, 어긋남 0** (6.4 표). 기대 반환 필드는 Q1 **20종** ·
      Q6 **9종** · Q7 **9종**이 기록에 전부 있고, Q1 은 S1~S8 **8행**을 한 줄씩 미성립으로 밟음,
      (d) 머리 표기 **3/3**.
      검증은 문서를 파싱해 대조하는 검사 25개로 실행했고 전부 통과했다 (슬라이스 1·2·3과 같이
      스크립트는 저장소에 두지 않았다 — 재현 방법은 6.6 "측정 방법"). 지어낸 반환 필드 0 ·
      정의서 어휘 밖 코드/`reason` 0 · 금지 필드 누출 0 · 최종 답 다섯 칸에 실패 코드 문자열 0.
      **발견** — 기록을 쓰다 정의서·패킷에 글자로 없는 자리 둘을 잡아 6.5 에 적었다:
      ① `file_review_request` 반환에 `corpusAsOf` 가 없어 최종 답 4번 칸(시점)을 무엇으로
      채울지 (→ 같은 세션 직전 읽기 반환에서 승계), ② Q7 은 읽기 도구를 안 부르므로
      `treatedAsData` 가 없어 "문구를 지시로 따르지 않았다"를 어디에 남길지 (→ 최종 답 3번 칸
      문장). **정의서와 문항은 고치지 않았다.** 값 표기는 두 등급으로 나눴다 — 코퍼스로 확인된
      값만 그대로 쓰고 나머지는 `⟨…⟩` 자리로 뒀다 (6.3). 확인 안 된 조문 번호를 기대 기록에
      박으면 이 프로젝트가 막으려는 실패를 설계 문서가 저지르게 된다.

- [x] **5. README 마무리와 루브릭 대조** — 설계 결정 요약(도구를 그만큼만 둔 이유 / 권한을
      나눈 기준 / 실패 뒤 행동을 그렇게 정한 근거) 각 2~3문장, "실행하지 않은 것과 이유",
      다음에 측정할 지점. 마지막에 제출 체크리스트 전 항목을 문서를 열어 확인.
      *완료 기준*: (a) 설계 결정 요약 3개가 각 2~3문장, (b) 실행하지 않은 것 4개(실측·회귀·
      MCP·배포)가 "채점 범위 밖" 사유와 함께 적힘, (c) README 체크리스트 전 항목 `[x]`,
      (d) PRD 6절 R1~R5·S1~S10 을 문서에서 하나씩 확인
      → **완료 2026-08-31.** 산출물: README "설계 결정 요약"(3소절) · "다음에 측정할 지점"(6행) ·
      "미해결로 남긴 것"(5건) · 제출 체크리스트 15항목.
      (a) 세 소절 각 3~5문장, (b) 실측·회귀·MCP·배포 4개가 "채점 범위 밖" 사유와 함께,
      (c) 체크리스트 **15/15 `[x]`**, 줄마다 확인 위치를 적었다,
      (d) **R1~R5·S1~S10 15개를 문서를 열어 하나씩 확인 — 미달 0.** 기계 대조 21검사 중
      20 통과(1건은 패킷 6.4 묶음표의 의도된 빈 첫 열 — 문서 결함이 아니라 검사기 오탐).
      슬라이스 1~4와 같이 스크립트는 저장소에 두지 않았다.
      **같이 정리한 낡은 표기 3건** — PRD 머리말이 `design-packet.md`·`PLAN.md` 를 "(작성 예정)"
      으로 가리키던 것, 정의서 머리말의 같은 표기, 패킷 머리말 "슬라이스 2 — ①③④⑤" 와
      현황 표의 뒤섞인 상태 열을 사실에 맞게 고쳤다. **본문 내용은 건드리지 않았다.**
      **미해결 5건은 고치지 않고 README 에 표로 남겼다** (사용자 결정 대기) — `foodType` enum
      커버리지, `REV-` 접두사 중복, 승인자가 요청자 본인인 것, `MISSING_EVIDENCE` 표기 불일치,
      슬라이스 3·4가 찾은 규칙 둘이 계약 본문에 없는 것.

- [x] **6. 미해결 4건 반영 — 접수 번호·승인 경계·표기 통일·판정 순서** `[어려움]` — 2026-08-31
      사용자 결정(2·3·4·5 고침, 1·6 유지)을 문서에 반영한다. 이미 검증을 마친 계약을 다시
      여는 작업이라 앞 슬라이스 산출물을 깨지 않는 것이 요건이다.
      **① 접수 번호 접두사** `REV-` → `REQ-` (m05 코퍼스 별표 종류 `REV` 와 뜻이 겹친다.
      코퍼스는 읽기 전용이라 이쪽을 바꾼다) — 정의서 5절 ⑤ · 트레이스 q6 · README.
      **② 승인자와 우회 기록의 경계** 승인자는 요청한 사용자 본인으로 **유지**하되 그 게이트를
      "내 내용을 사람에게 넘겨도 되는가"라는 **동의 게이트**로 성격을 명시하고,
      `POLICY_BYPASS_ATTEMPT` 를 `file_review_request` 의 `reason` 에서 **뺀다** — 우회 시도
      기록은 판단을 넘기는 이관이 아니라 사건 기록이고, 도구가 아니라 **운영 층**이 승인 없이
      남긴다 (PRD 5절 운영 층). 게이트에 예외를 뚫지 않고 모순이 사라진다.
      **③ `MISSING_EVIDENCE` 표기 통일** 정의서 8절을 패킷 3.1 에 맞춰 1차 행동 = **보류**,
      후속 = 자기 교정(읽기 도구 선행). 행동 어휘는 정의서 1.4 의 넷뿐이다.
      **④ 판정 순서를 계약 본문으로** `foodType` 이 enum 밖이면 `outOfScope` 고정 목록을
      **먼저** 대조해 걸리면 `OUT_OF_SCOPE`, 아니면 `TYPE_NOT_COVERED`. 이것이 없으면
      탁주(범위 밖)와 된장(자료 없음)이 구별되지 않는다.
      *완료 기준*: (a) `REV-20` 이 저장소에 0건이고 `REQ-` 로 바뀐 자리가 3곳, (b)
      `POLICY_BYPASS_ATTEMPT` 가 `file_review_request` 의 `reason` enum 과 '쓰는 때'에서
      빠지고, 문항 Q7 과 트레이스 q7 의 세 칸이 새 경계와 일치, (c) `MISSING_EVIDENCE` 의
      1차 행동이 정의서·패킷 양쪽에서 `보류` 로 같고 패킷 3.2 의 25개 미지정 0 이 유지됨,
      (d) 판정 순서가 정의서 3절과 패킷 3절에 글자로 있고 Q5·Q8 기대 코드와 모순 없음,
      (e) **루브릭 R1~R5 와 PRD S1~S10 이 여전히 전부 통과** — 특히 R1(공격 시도 문항)이
      Q7 변경으로 깨지지 않았음을 확인. (f) README "미해결로 남긴 것" 표에서 2·3·4·5 를
      내리고 1·6 만 "사용자가 이대로 두기로 함"으로 남김
      → **완료 2026-08-31.** 고친 파일: 정의서(3절 ⑥ 판정 순서 · 5절 ③④⑤⑥ · 8절) ·
      패킷(2.3~2.5 · 3.2~3.4 · 5.1/5.2 Q7/5.4/5.5/5.6 · 6.1/6.2/6.4/6.5/6.6 · 7절) ·
      트레이스(q7 전면 재작성 · q6 4곳 · q1 말미) · README(미해결 표 재구성 · 체크리스트).
      (a) `REV-20` 산출물 6파일에서 0건(이 완료 기준 문구 자체만 남음), `REQ-` 는 PLAN 이
      지목한 3곳(정의서 5절 ⑤ · 트레이스 q6 · README) 전부 + 패킷 재대조 주석,
      (b) `reason` enum **4종** — Q7 기대 도구가 "도구 호출 0회 + 운영 층 기록", q7 여섯 칸
      재작성, 문항↔기록 대조(패킷 6.4) 어긋남 0,
      (c) 정의서 5절 ⑥·8절 = 1차 **보류** + 후속 자기 교정, 패킷 3.2 **25행 · 되묻기 9 ·
      보류 14 · 재시도 2** 유지,
      (d) 정의서 3절 ⑥ "enum 밖 판정 순서" + 패킷 3.2 "#7 과 #16 의 판정 순서",
      Q5 2턴 `TYPE_NOT_COVERED` · Q8 `OUT_OF_SCOPE` 와 모순 0,
      (e) R1~R5 · S1~S10 **15개 재확인 미달 0** — R1 은 Q7 이 거절·기록·확정 금지·이관 경로
      제안의 뼈대를 유지, (f) README 미해결 표 = **1·6 두 행**("사용자가 이대로 두기로 함").
      검증은 문서를 파싱해 대조하는 검사 40개로 실행했고 전부 통과했다 (앞 슬라이스와 같이
      스크립트는 저장소에 두지 않았다).

## 검증

실행되는 코드가 없으므로 **문서를 열어 대조**하는 것이 검증이다.

1. "빈 칸 0" 항목은 `grep` 으로 미기입 표시(`[결정 필요]`·`TODO`·빈 셀)를 세어 확인한다
2. 도구쌍 '쓰는 때' 겹침(슬라이스 1)과 문항↔도구 모순(슬라이스 3)은 표를 나란히 놓고 읽는다
3. 슬라이스 5에서 PRD 6절의 **R1~R5**(루브릭)와 **S1~S10**(과제 안내)을 한 줄씩 확인한다
