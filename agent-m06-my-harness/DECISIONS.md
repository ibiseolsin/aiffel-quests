# 설계 결정 기록

OPEN 미결정 / CHOSEN 선택 / DEFERRED 합의해 후속으로 미룸. 추천은 선택이 아니다.
결정일 2026-09-09. 사용자 답변은 세션에서 직접 받은 것과, 되돌리기 쉬운 결정을 위임받아 정한 것을 구분해 적는다.

| ID | 결정 | 상태 | 선택과 답 | 이유·영향 |
|---|---|---|---|---|
| D01 | 사용자/업무 | CHOSEN | 수강생 본인. 과제 폴더의 작은 fixture 를 (a) 읽고 근거 있는 요약, (b) 실패 테스트 수정 | 12강 A01(자료 읽기)·A03(코드 수정) 두 검증이 요구하는 최소 조합. fixture 를 내가 만들어 정답을 알고 있으므로 "근거 일치" 를 대조할 수 있다 |
| D02 | 플랫폼 | CHOSEN | **CLI**, 화면과 도구 실행 모두 로컬 한 대 (Windows 11 / Python 3.13.15) | 웹 UI 는 승인·세션 검증에 아무 것도 더해 주지 않고 HTTP 계약이라는 검증 부담만 늘린다. INTERFACES 의 작업 계약을 CLI 옵션에 1:1 로 대응시켰다 |
| D03 | 언어 | CHOSEN | **Python 3.13** | 벤치마크 어댑터가 `async def solve_task` 인 Python 함수다. 다른 언어면 어댑터가 서브프로세스를 띄우고 종료 코드·시간 제한·사용량 미관측을 따로 처리해야 한다 — 같은 기간에 검증할 표면이 늘어난다. 벤치마크 환경의 `httpx` 를 그대로 쓰므로 의존성도 늘지 않는다 |
| D04 | 첫 provider/모델 | CHOSEN | **Ollama 네이티브 `/api/chat`, `qwen3.5:2b`** (사용자 답변) | 키가 필요 없고 이 머신에 이미 있다. 도구 호출·`thinking`·`prompt_eval_count`/`eval_count` 를 확인했다(2026-09-09). 대가: 2.3B 모델이라 hard 문항 점수는 낮게 나온다 — 점수 자체가 완료 조건이 아니므로 받아들인다 |
| D05 | 최초 도구/폴더/테스트 범위 | CHOSEN | 읽기 `list_files`·`read_file` → 쓰기 `write_file`(승인) → 실행 `run_python`·`run_tests`. 폴더는 `--workspace` 하나. 테스트는 `--test-target` 로 **미리 고정** | 임의 셸을 열지 않는 것이 R05 의 핵심. 테스트 대상을 모델이 정하면 "테스트를 바꾸지 말라" 를 코드로 보장할 수 없다 |
| D06 | 승인 방식 | CHOSEN | 파일 변경마다 **통합 diff + 요약(줄 수·바이트)** 을 보여 주고 `y/n`. 승인은 `(작업 id, 도구 호출 id, 변경 내용 sha256)` 에 묶인다. 거절 시 그 호출만 실패로 모델에 돌려주고 반복은 계속 | 사용자가 본 내용이 바뀌면 그 승인을 재사용할 수 없다. 거절을 작업 실패로 끝내면 모델이 다른 방법을 시도할 기회가 없다 |
| D07 | 세션/저장 | CHOSEN | **파일 영속** `sessions/<name>.json`. 매 단계 원자적 저장(temp→replace). 설정 불일치면 복원 거부 | 재시작 복원까지 검증하고 싶었다 → A09 를 DEFERRED 로 두지 않는다. 저장 내용은 모델에 보이는 대화 그대로이므로 fixture 내용이 들어간다 — 공개물에서 제외한다 |
| D08 | 추가 provider | **DEFERRED** | OpenAI Responses 어댑터를 만들지 않았다 | `OPENAI_API_KEY` 가 없어 **실행으로 검증할 수 없다.** 검증 못 한 코드를 제출물에 넣으면 A10 을 "PASS 인 척" 하게 된다. `Provider` 프로토콜(`complete(messages, tools) -> ModelTurn`)로 자리만 분리해 뒀다. → A10 `DEFERRED (D08)` |
| D09 | 확장 기능 | **DEFERRED** | 취소·동시 실행·원격 접속을 넣지 않았다. 진행 이벤트(JSONL trace)는 넣었다 | 첫 버전에서 검증할 표면을 늘리지 않는다. 진행 이벤트는 R01(진행·실패 구분)과 실패 분석에 필요해 예외로 포함 |

## 결정 변경

| 날짜 | 변경한 결정 | 변경 이유 | 영향받는 요구·계약·검증 |
|---|---|---|---|
| 2026-09-09 | 벤치마크 실행 한도를 기본값(40단계·300초·10,000토큰)에서 조정 | `qwen3.5:2b` 는 모델 호출 1회가 10~30초다. 기본값이면 10문항 전부 시간 한도에 걸려 `error` 로만 끝나고 실패 원인을 구분할 수 없다 | R08 / A11·A12 — 조정한 값은 `results/run-metadata.json` 에 남고 **기준·개선 두 실행에 동일하게** 적용한다 |
